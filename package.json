{
  "name": "eduard-ltd.com",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./ && cp robots.txt dist/ && cp sitemap.xml dist/ && cp edlogobluru.png dist/",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.12.0",
    "sharp": "^0.33.5",
    "gh-pages": "^6.x.x"
  }
}
